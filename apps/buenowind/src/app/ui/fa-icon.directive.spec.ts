import { FaIconDirective } from "./fa-icon.directive";

describe("FaIconDirective", () => {
    it("should create an instance", () => {
        const directive = new FaIconDirective();
        expect(directive).toBeTruthy();
    });
});
