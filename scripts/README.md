# Scripts

## Executing Bash Scripts

Before executing any bash script in this directory:

1. Make the script executable from root: `chmod +x scripts/<script-name>.sh`
2. Run the script from root: `bash scripts/<script-name>.sh`
